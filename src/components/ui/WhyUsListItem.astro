---
interface Props {
  title: string;
  description: string;
  icon?: string;
}

const { title, description, icon = "" } = Astro.props;
const hasIconSlot = Astro.slots.has("icon");
---

<article class="flex gap-4 p-4 sm:p-5">
  <div
    class="mt-1 flex h-12 w-12 shrink-0 items-center justify-center rounded-lg bg-accent-secondary text-accent-primary"
    aria-hidden="true"
  >
    {
      hasIconSlot ? (
        <slot name="icon" />
      ) : icon ? (
        <img src={icon} alt="" class="h-6 w-6" loading="lazy" />
      ) : null
    }
  </div>

  <div class="flex flex-col gap-1">
    <h3 class="text-lead font-semibold text-primary">{title}</h3>
    <p class="text-body text-secondary">{description}</p>
  </div>
</article>
