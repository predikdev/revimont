---
const steps = [
  {
    number: "01",
    title: " Domluva termínu a rozsahu revize",
    description:
      "Kontaktujte nás telefonicky nebo e-mailem a společně domluvíme vhodný termín a upřesníme, jaký typ revize potřebujete.",
  },
  {
    number: "02",
    title: " Provedení revize na místě",
    description:
      "Revizní technik provede důkladnou kontrolu elektroinstalace přímo u vás. Zkontrolujeme stav rozvodů, jističů a dalších komponent.",
  },
  {
    number: "03",
    title: "Vyhodnocení stavu a revizní zpráva",
    description:
      "Na základě kontroly vypracujeme kompletní revizní dokumentaci. Obdržíte přehlednou zprávu s vyhodnocením stavu.",
  },
];
---

<section id="revize-process" class="bg-background-dark">
  <div class="container">
    <div class="flex flex-col gap-3 max-w-xl">
      <p class="uppercase text-accent-primary text-small tracking-wide">Proces</p>
      <h2 class="font-playfair font-bold text-heading-2 text-primary">Jak revize probíhá</h2>
      <p class="text-body text-secondary">Jednoduchý a transparentní proces v třech krocích</p>
    </div>

    <ul role="list" class="mt-14 grid md:grid-cols-2 lg:grid-cols-3 gap-10">
      {
        steps.map((step) => (
          <li class="flex flex-col gap-10 py-10 max-w-xs">
            <span class="text-7xl font-bold text-accent-secondary">{step.number}</span>
            <div class="flex flex-col gap">
              <h3 class="text-lg font-semibold text-primary">{step.title}</h3>
              <p class="mt-3 text-body text-secondary">{step.description}</p>
            </div>
          </li>
        ))
      }
    </ul>
  </div>
</section>
