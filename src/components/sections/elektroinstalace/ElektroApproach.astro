---
import SectionWrapper from "../../ui/SectionWrapper.astro";
import SectionHeader from "../../ui/SectionHeader.astro";
import { MessageCircle, Wrench, Handshake, Check } from "@lucide/astro";

const steps = [
  {
    number: "01",
    icon: MessageCircle,
    title: "Konzultace a návrh",
    description: "Osobně probereme vaše požadavky, prohlédneme prostory a připravíme transparentní cenovou nabídku s termínem realizace.",
    bullets: ["Osobní prohlídka místa", "Návrh variant řešení", "Jasná cenová kalkulace"],
  },
  {
    number: "02",
    icon: Wrench,
    title: "Precizní realizace",
    description: "Instalaci provádíme čistě a přesně, s použitím ověřených komponentů od renomovaných značek. Průběžně kontrolujeme kvalitu.",
    bullets: ["Ověřené materiály a komponenty", "Průběžná kontrola kvality", "Minimální zásah do interiéru"],
  },
  {
    number: "03",
    icon: Handshake,
    title: "Předání a servis",
    description: "Dílo předáme plně funkční včetně revizní zprávy. Zaškolíme obsluhu a poskytujeme záruční i pozáruční servis.",
    bullets: ["Revizní zpráva k předání", "Zaškolení obsluhy", "Záruční a pozáruční podpora"],
  },
];
---

<SectionWrapper background="light" withGrid>
  <SectionHeader
    eyebrow="Jak pracujeme"
    title="Jak probíhá elektroinstalace"
    description="Od prvního kontaktu po předání. Tři kroky, žádné překvapení."
    background="light"
    descWidth="narrow"
  />

  <div class="grid md:grid-cols-3 gap-6 mt-12">
    {steps.map((step) => (
      <div class="group relative bg-white rounded-xl border border-neutral-200 p-8 shadow-sm transition-shadow duration-200 hover:shadow-md">
        <span class="absolute top-6 right-6 font-sora text-5xl font-bold text-neutral-100 leading-none select-none transition-colors duration-300 group-hover:text-accent-primary/40" aria-hidden="true">
          {step.number}
        </span>

        <div class="w-12 h-12 bg-accent-primary/10 rounded-full flex items-center justify-center mb-6 transition-colors duration-300 group-hover:bg-accent-primary/[0.18]" aria-hidden="true">
          <step.icon class="w-5 h-5 text-accent-primary transition-transform duration-300 group-hover:scale-110" />
        </div>

        <h3 class="font-sora text-xl font-bold text-neutral-900 mb-3">{step.title}</h3>
        <p class="text-sm text-neutral-600 leading-relaxed mb-6">{step.description}</p>

        <ul class="space-y-2">
          {step.bullets.map((bullet) => (
            <li class="flex items-center gap-3">
              <span class="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-accent-primary/10 border border-accent-primary/30" aria-hidden="true">
                <Check class="h-3 w-3 text-accent-primary" />
              </span>
              <span class="text-sm font-medium text-neutral-700">{bullet}</span>
            </li>
          ))}
        </ul>
      </div>
    ))}
  </div>
</SectionWrapper>
