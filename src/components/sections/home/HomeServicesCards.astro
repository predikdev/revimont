---
import SectionWrapper from "../../ui/SectionWrapper.astro";

import { Zap, ShieldCheck, Wrench } from "@lucide/astro";

const services = [
  {
    number: "01",
    title: "Elektroinstalace",
    description:
      "Kompletní návrh a realizace elektrických rozvodů pro novostavby, rekonstrukce i komerční objekty.",
    link: "/sluzby/elektroinstalace",
    icon: Zap,
    bullets: ["Novostavby", "Rekonstrukce", "Bytové domy", "Rozvaděče"],
  },
  {
    number: "02",
    title: "Revize a kontroly",
    description:
      "Pravidelné revize elektroinstalací a zařízení dle platné legislativy včetně vystavení revizních zpráv.",
    link: "/sluzby/revize",
    icon: ShieldCheck,
    bullets: ["Výchozí revize", "Pravidelné revize", "Revize hromosvodů", "Kontrola bezpečnosti"],
  },
  {
    number: "03",
    title: "Montáže a opravy",
    description:
      "Opravy poruch, údržba a modernizace stávajících elektrických instalací včetně havarijního servisu.",
    link: "/sluzby",
    icon: Wrench,
    bullets: ["Domácí telefony", "Zvonková tabla", "Rozvody domáchích telefonů", "Modernizace systémů"],
  },
];
---

<SectionWrapper background="light" withGrid>
  <!-- Section header -->
  <div class="mb-14 md:mb-18">
    <!-- Eyebrow -->
    <div class="mb-5 flex items-center gap-3">
      <span aria-hidden="true" class="h-px w-8 bg-accent-primary"></span>
      <span class="text-sm font-semibold uppercase tracking-widest text-accent-primary">
        Naše služby
      </span>
    </div>

    <!-- Title with underline decoration -->
    <h2 class="font-sora text-4xl font-bold leading-tight text-neutral-800 md:text-5xl">
      Komplexní řešení pro<br />
      <span class="relative inline-block pb-3">
        vaši nemovitost
        <span
          aria-hidden="true"
          class="absolute bottom-0 left-0 h-[3px] w-full rounded-full bg-accent-primary"
        ></span>
      </span>
    </h2>

    <!-- Description -->
    <p class="mt-8 max-w-2xl text-base leading-relaxed text-neutral-800/60 md:text-lg">
      Nabízíme profesionální elektrikářské služby od návrhu až po realizaci. Každý projekt
      řešíme s důrazem na kvalitu, bezpečnost a dlouhodobou spolehlivost.
    </p>
  </div>

  <!-- Cards grid -->
  <div class="grid gap-6 md:grid-cols-2 xl:grid-cols-3">
    {
      services.map((service) => (
        <article class="group relative overflow-hidden rounded-xl border border-neutral-200 bg-white p-8 shadow-sm transition-shadow duration-200 hover:shadow-md md:p-10">
          <!-- Orange top accent line -->
          <span aria-hidden="true" class="absolute left-1/2 top-0 h-[3px] w-4/5 -translate-x-1/2 bg-accent-primary opacity-0 transition-opacity duration-300 group-hover:opacity-100" />

          <!-- Number badge -->
          <span aria-hidden="true" class="absolute right-7 top-6 font-sora text-sm font-bold tracking-wide text-accent-primary/30 transition-colors duration-300 group-hover:text-accent-primary/50">
            {service.number}
          </span>

          <!-- Icon -->
          <div aria-hidden="true" class="mb-7 inline-flex h-13 w-13 items-center justify-center rounded-xl bg-accent-primary/10 text-accent-primary transition-colors duration-300 group-hover:bg-accent-primary/[0.16]">
            <service.icon class="h-5 w-5 transition-transform duration-300 group-hover:scale-110" />
          </div>

          <!-- Title -->
          <h3 class="font-sora text-xl font-bold text-neutral-800 md:text-2xl">{service.title}</h3>

          <!-- Description -->
          <p class="mt-3 text-[0.9375rem] leading-relaxed text-neutral-800/55">{service.description}</p>

          <!-- Divider -->
          <hr aria-hidden="true" class="my-6 border-neutral-300/50" />

          <!-- Bullets -->
          <ul class="space-y-3">
            {service.bullets.map((bullet) => (
              <li class="flex items-center gap-3 text-sm text-neutral-800/65">
                <span aria-hidden="true" class="h-2 w-2 shrink-0 rounded-full bg-accent-primary" />
                <span>{bullet}</span>
              </li>
            ))}
          </ul>

          <!-- CTA link -->
          <a
            href={service.link}
            aria-label={`Zjistit více o ${service.title}`}
            class="group/link mt-8 inline-flex items-center gap-1.5 text-sm font-semibold text-accent-primary"
          >
            <span aria-hidden="true">Zjistit více</span>
            <span aria-hidden="true" class="transition-transform duration-200 group-hover/link:translate-x-0.5">
              →
            </span>
          </a>
        </article>
      ))
    }
  </div>
</SectionWrapper>
