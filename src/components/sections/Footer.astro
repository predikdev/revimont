---
import Logo from "../ui/Logo.astro";

const year = new Date().getFullYear();

const navigationLinks = [
  { href: "/", label: "Domů" },
  { href: "/sluzby", label: "Elektroinstalace" },
  { href: "/sluzby", label: "Revize" },
  { href: "/sluzby", label: "Montáže a opravy" },
];

const companyLinks = [
  { href: "/#why-us", label: "O nás" },
  { href: "/galerie", label: "Realizace" },
  { href: "/kontakt", label: "Kontakt" },
  { href: "/ochrana-udaju", label: "Ochrana údajů" },
];

const mapSrc =
  "https://www.google.com/maps?q=K%20Leti%C5%A1ti%20487,%20339%2001%20Klatovy%20II,%20%C4%8Cesko&output=embed";
---

<footer class="bg-background-dark text-primary">
  <div class="container py-20">
    <div class="mx-auto max-w-2xl text-center">
      <p class="uppercase text-accent-primary text-sm font-medium">Kde nás najdete</p>
      <h2 class="mt-3 font-playfair text-heading-2 font-bold">Sídlíme v Klatovech</h2>
      <p class="mt-3 text-secondary">
        Působíme v celém Plzeňském kraji. Pro větší projekty vyjedeme i do jiných regionů.
      </p>
    </div>

    <div class="mt-10">
      <div class="overflow-hidden rounded-2xl border border-white/10 shadow-2xl shadow-black/40">
        <iframe
          title="Mapa: K Letišti 487, Klatovy"
          src={mapSrc}
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          class="h-80 w-full md:h-105"
          allowfullscreen></iframe>
      </div>
      <!-- <p class="mt-4 text-center text-sm text-secondary">K Letišti 487, 339 01 Klatovy II, Česko</p> -->
    </div>

    <div class="mt-16 grid gap-10 lg:grid-cols-[1.4fr_0.8fr_0.8fr]">
      <div class="space-y-4 flex flex-col gap-1">
        <Logo class="w-40" />
        <p class="text-secondary text-sm leading-relaxed max-w-sm">
          Elektroinstalace, revize, montáže a opravy v Klatovech a okolí. Práce provádíme v bytech,
          rodinných domech i ve firemních objektech. Spolehlivý partner s více jak 15 lety
          zkušeností.
        </p>
      </div>

      <div class="space-y-4">
        <p class="text-base font-semibold">Navigace</p>
        <ul class="space-y-2 text-sm text-secondary">
          {
            navigationLinks.map((link) => (
              <li>
                <a href={link.href} class="transition-colors hover:text-accent-primary">
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div class="space-y-4">
        <p class="text-base font-semibold">Společnost</p>
        <ul class="space-y-2 text-sm text-secondary">
          {
            companyLinks.map((link) => (
              <li>
                <a href={link.href} class="transition-colors hover:text-accent-primary">
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <div
      class="mt-12 flex flex-col gap-4 border-t border-white/15 pt-6 text-xs text-secondary md:flex-row md:items-center md:justify-between"
    >
      <p>© {year} Všechna práva vyhrazena | Revimont Klatovy s.r.o. | Klatovy</p>
      <p>IČ | DIČ</p>
    </div>
  </div>
</footer>
